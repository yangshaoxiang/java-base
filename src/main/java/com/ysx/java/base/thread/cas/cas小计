小计
CAS有3个操作数，内存值V，旧的预期值A，要修改的新值B。
当且仅当预期值A和内存值V相同时，将内存值V修改为B，否则什么都不做
1. cas 本身在 java 中的实现最终需要调用cpu的原语指令
2. 使用 cas 操作的变量一般是用 volatile 修饰因为，使用cas一般是在并发场景，并发场景需要修改的数据对其他线程可见
3. 不可过度使用 cas 因为承上，cas操作的变量都是 volatile 修饰，volatile 可见性的实现机制是 MESI协议和总线嗅探机制，
   过多的 cas 会导致总线带宽用尽，造成总线风暴